import { OrdenarContactosPipe } from './ordenar-contactos.pipe';

describe('OrdenarContactosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarContactosPipe();
    expect(pipe).toBeTruthy();
  });
});
